/**
 * Back-compat entry: same process as `api-server.ts`.
 * MCP Streamable HTTP: GET|POST /mcp
 * REST: /api/*  ·  Static SPA: public/  ·  Health: GET /health
 *
 * Run: npm run dev:http  (or npm run dev:api)
 */
import "./api-server.js";
