export const FUNNEL_SANKEY_FLOW_LIMIT = 8;
export const FUNNEL_REACH_DISPLAY_CAP = 100_000;

/** Default pipeline stage (name match, case-insensitive) pre-selected in Funnel Alluvial when none chosen yet. */
export const DEFAULT_FUNNEL_ALLUVIAL_PIPELINE_STAGE_NAME = "Opportunity";

/** 1-based column index where that stage is inserted in the alluvial sequence (see pipeline stage positions UI). */
export const DEFAULT_FUNNEL_ALLUVIAL_PIPELINE_STAGE_POSITION = 6;
